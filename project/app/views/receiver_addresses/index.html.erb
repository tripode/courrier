<h1>Listing receiver_addresses</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Label</th>
    <th>Address</th>
    <th>City</th>
    <th>Receiver</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @receiver_addresses.each do |receiver_address| %>
  <tr>
    <td><%= receiver_address.id %></td>
    <td><%= receiver_address.label %></td>
    <td><%= receiver_address.address %></td>
    <td><%= receiver_address.city_id %></td>
    <td><%= receiver_address.receiver_id %></td>
    <td><%= link_to 'Show', receiver_address %></td>
    <td><%= link_to 'Edit', edit_receiver_address_path(receiver_address) %></td>
    <td><%= link_to 'Destroy', receiver_address, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Receiver address', new_receiver_address_path %>
