<h1>Lista de Destinatarios</h1>

<table>
  <tr>
    <th>Nombre y Apellido</th>
    <th>Documento</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @receivers.each do |receiver| %>
  <tr>
    <td><%= receiver.receiver_name %></td>
    <td><%= receiver.document %></td>
    <td><%= link_to 'Show', receiver %></td>
    <td><%= link_to 'Edit', edit_receiver_path(receiver) %></td>
    <td><%= link_to 'Destroy', receiver, confirm: 'Estas seguro?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo Destinatario', new_receiver_path %>
